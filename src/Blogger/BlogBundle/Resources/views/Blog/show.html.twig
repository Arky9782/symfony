{% extends 'BlogBundle:layouts:master.html.twig' %}

{% block content %}


    <h1>{{ result.title }}</h1>
    <hr>
    <p>{{ result.body }}</p>

    <br>

    <br>

{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}

{% endblock %}