{% extends 'BlogBundle:layouts:master.html.twig' %}

{% block content %}
{% for item in result %}

    <h1><a href="/posts/{{ item.id }}"> {{ item.title }}</a></h1>
    <p>{{ item.created }}</p>
    <p>{{ item.body }}</p>
    <hr>

    {% endfor %}

{% endblock %}